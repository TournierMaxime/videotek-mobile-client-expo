const container = {
    flex: 1
}

const accordContainer = {
    paddingBottom: 4
}

const accordHeader = {
    padding: 12,
    flex: 1,
    flexDirection: 'row',
    justifyContent:'space-between'
}

const accordTitle = {
    fontSize: 16,
}

const accordBody = {
    padding: 0
}

const textSmall = {
    fontSize: 16
}

const seperator = {
    height: 12
}

export default { 
    container,
    accordContainer,
    accordHeader,
    accordTitle,
    accordBody,
    textSmall,
    seperator
}