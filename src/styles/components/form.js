const formContainer = {
    backgroundColor: '#fff',
    padding: 20,
    borderRadius: 10,
    margin: 20,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 5,
    position: 'relative'
}

const formInput = {
    backgroundColor: '#f2f2f2',
    padding: 10,
    borderRadius: 5,
    marginBottom: 10,
}

const formLabel = {
    fontSize: 14,
    fontWeight: 'bold',
    marginBottom: 10,
}

export default { formLabel, formInput, formContainer, }